[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis-shared"
version = "0.1.0"
description = "AEGIS shared event/Firestore contracts and guardrail pipeline"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.6",
]

[tool.setuptools.packages.find]
include = ["schemas*", "guardrails*"]
